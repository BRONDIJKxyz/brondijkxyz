# Homepage trial

Build a separate, static premium homepage at `/trial/`. Preserve the existing homepage and shared styles/scripts. Use existing project facts and assets, oversized typography, deliberate visual hierarchy, responsive layouts and accessible interactions. Success: real project destinations, working filters and internal-tool dialogs, keyboard support, reduced-motion support, no missing local assets, and a direct link to compare the original. No production deployment is included.

Stack: standalone HTML, CSS and vanilla JavaScript; no build dependencies. This isolated prototype has its own layout rather than migrating the existing multipage site to a shared template.
